# docker compose settings for a public site – copy to .env (git-ignored)
DOMAIN=plan.example.com
AUTH_USERNAME=admin
AUTH_PASSWORD=Parmanbabra03
# python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=replace-with-64-hex-chars
SESSION_HOURS=168

